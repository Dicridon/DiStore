#include "compute_node.hpp"

namespace DiStore {
    namespace Memory {
        auto ComputeNodeAllocator::allocate(size_t sz) -> byte_ptr_t {
 
        }

        auto ComputeNodeAllocator::free(RemotePointer chunk) -> void {
            
        }
    }
}
